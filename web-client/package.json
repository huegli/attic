{
  "name": "attic-web-client",
  "version": "0.1.0",
  "private": true,
  "description": "Web browser client for the Attic Atari 800 XL emulator",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
